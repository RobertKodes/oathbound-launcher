Save the Oathbound Realms logo here as:

    oathbound-logo.png

(the exact image you shared — a transparent PNG works best). The launcher uses it
as the login logo, the top-bar mark, and a faint background watermark.

Optional: drop a custom backdrop image here too if you want one later. Until the
logo file exists, the launcher falls back to a styled text title automatically.
